# Backend Server Configuration
PORT=5000
NODE_ENV=development
CLIENT_URL=http://localhost:3000

# MongoDB Connection
# Set MONGODB_URI to connect to MongoDB Atlas or local mongod.
# If omitted or left empty, MongoMemoryServer is automatically provisioned.
MONGODB_URI=

# Authentication Secrets
JWT_SECRET=ams_super_secure_enterprise_jwt_secret_key_2026
JWT_EXPIRES_IN=7d

# Business Timezone
TIMEZONE=Asia/Kolkata
